{
  "name": "markdown-novels-framework",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && cp -r docs dist/docs",
    "preview": "vite preview",
    "lint": "oxlint .",
    "format": "oxfmt --write ."
  },
  "dependencies": {
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0",
    "sharp": "^0.35.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
